(ns demo-provider-result)

(defn main []
  (let [buf (alloc 8)
        result (http-fetch (str-ptr "GET /fail") (str-len "GET /fail") buf 8)]
    (if (result-err? result)
      (- 0 result)
      result)))
