(ns demo-result-record)

(defn main []
  (let [buf (alloc 8)
        record (alloc 8)
        result (http-fetch (str-ptr "GET /record") (str-len "GET /record") buf 8)
        written (result-write! record result)]
    (+ (result-status written)
       (- 0 (result-value written)))))
