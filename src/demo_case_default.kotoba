(ns demo-case-default)

;; `(case 5 1 10 2 20 99)`: dispatch value 5 matches none of the two
;; clauses, so `main` falls through to the trailing default (99).
(defn main []
  (case 5
    1 10
    2 20
    99))
