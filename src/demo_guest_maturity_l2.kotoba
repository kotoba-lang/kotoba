;; ADR-2607180900 L2 demo: multi-body when + do sequencing (desugar only).
;; Pure compute — no host imports. Proves emit → run returns 42.
(ns demo-guest-maturity-l2)

(defn main []
  (when 1
    (do 7 11)
    (do 0 42)))
