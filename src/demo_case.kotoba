(ns demo-case)

;; `(case 2 1 10 2 20 3 30 99)`: dispatch value 2 matches the middle clause,
;; so `main` returns 20 -- the unmatched clauses (1/10, 3/30) and the
;; trailing default (99) must never be selected.
(defn main []
  (case 2
    1 10
    2 20
    3 30
    99))
