(ns demo-real-fs)

(defn main []
  (let [w (fs-write (str-ptr "state.txt") (str-len "state.txt") (str-ptr "data") (str-len "data"))
        r (fs-read (str-ptr "state.txt") (str-len "state.txt") (alloc 16) 16)]
    r))
