(ns demo-real-keychain)

(defn main []
  (let [w (keychain-write (str-ptr "token") (str-len "token") (str-ptr "secret") (str-len "secret"))
        r (keychain-read (str-ptr "token") (str-len "token") (alloc 16) 16)]
    r))
