{:kotoba.policy/capabilities #{:clipboard/text
                               :http/fetch
                               :keychain/text
                               :fs/app-data}
 ;; Multi-provider ABI demo (not an SSRF test). Explicit opt-out of the
 ;; safe-default network allowlist so the synthetic "GET /" URL still links.
 :kotoba.policy/http-require-allowlist false}
