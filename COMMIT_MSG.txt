runtime: resync vendored grammar + advance kotoba-lang/kotoba-sema pins (fs-browse-dir)

Grammar resync wave for the kotoba-lang/find recursive tree-walk
(capability id 261 "fs/browse-dir"):

- Both vendored copies of lang/guest-grammar.edn (resources/ and
  vendor/grammar/resources/) resynced to the authority's fs-browse-dir
  head addition.
- guest_grammar_vendor_test digest pin advanced to the new grammar sha256
  (ee7ea37c), moved together with the kotoba-lang (cb41cb62) and
  kotoba-sema (af8cc780) pins per the shared digest-carry rule.

Remaining q8-report / authority-cases failures are pre-existing (present
on pristine main, measured before and after) and unrelated to this resync.