name: Native CLI release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  native:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            platform: darwin-arm64
          - os: macos-15-intel
            platform: darwin-amd64
          - os: ubuntu-24.04
            platform: linux-amd64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: graalvm/setup-graalvm@v1
        with:
          java-version: "21"
          distribution: graalvm
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: true
      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest
      - run: chmod +x scripts/build-native.sh && scripts/build-native.sh
      - name: Package
        run: |
          mkdir -p "target/package"
          cp target/native/kotoba LICENSE README.md "target/package/"
          tar -C target/package -czf "target/kotoba-${{ matrix.platform }}.tar.gz" kotoba LICENSE README.md
          shasum -a 256 "target/kotoba-${{ matrix.platform }}.tar.gz" > "target/kotoba-${{ matrix.platform }}.tar.gz.sha256"
      - uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            target/kotoba-${{ matrix.platform }}.tar.gz
            target/kotoba-${{ matrix.platform }}.tar.gz.sha256
