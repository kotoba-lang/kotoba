;; The SAME script with :git/run withheld. The guest still requires wire 22,
;; so admission must refuse the whole run before a git process starts or a
;; byte is written.
{:kotoba.policy/capabilities #{:fs/app-data}
 :kotoba.policy/forbid-wildcard true
 :kotoba.policy/capability-resources
 {:fs/app-data #{"test/fixtures/kbb_ports/out"}}}