;; The SAME script with :git/run and :fs/transact withheld. The guest still
;; requires wire 22 and wire 19, so admission must refuse the whole run
;; before a byte is written or a git process starts.
{:kotoba.policy/capabilities #{:fs/app-data}
 :kotoba.policy/forbid-wildcard true
 :kotoba.policy/capability-resources
 {:fs/app-data #{"test/fixtures/kbb_ports/out"}}}
