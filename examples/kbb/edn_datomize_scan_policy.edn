;; Deny-by-default: ONE capability, scoped to the ONE fixture directory this
;; example reads. Reading EDN needs no capability of its own -- only the
;; bytes do.
{:kotoba.policy/capabilities #{:fs/app-data}
 :kotoba.policy/forbid-wildcard true
 :kotoba.policy/capability-resources
 {:fs/app-data #{"test/fixtures/kbb_ports/datomize_dir"}}}
