apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kotoba-sled
  namespace: kotoba
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vultr-block-storage-hdd-retain
  resources:
    requests:
      storage: 40Gi
