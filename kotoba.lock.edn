{:kotoba.lock/version 1
 ;; No dependencies -- this repo's own src/demo_*.kotoba examples are
 ;; zero-dependency. `wasm emit`/`wasm run --package-lock` (F-001,
 ;; mandatory, no opt-out) still needs a lock file to exist even for a
 ;; dependency-free build; :deps [] is vacuously admitted
 ;; (kotoba-lang/kotoba-lang#11). See docs/lang/gates.md's "Package
 ;; admission" section.
 :deps []}
