{:post/id "kotoba-clojureverse-launch"
 :post/status :pending-review
 :post/created "2026-07-08"
 :post/posted-at "2026-07-08"
 :post/posted-note "Posted to ClojureVerse (Showcase). Currently pending
                    moderator/first-post review, not yet publicly visible.
                    Fill in :post/url once the topic clears review and has
                    a permalink."
 :post/ready-note "Both publish-gate items closed 2026-07-08. Body-markdown
                   rewritten to state each as shipped (with its documented
                   scope/limitation), not as roadmap. Do one final read
                   before posting to confirm no stale phrasing slipped
                   through."

 ;; --- publish gate (closed) -------------------------------------------
 ;; Kept for the record; both items are :done and the body-markdown below
 ;; reflects the shipped state, not the pre-implementation framing.
 :post/blocked-by
 [{:item "Rust-style borrow checker (S2 layer)"
   :ref "kotoba-lang/kotoba-lang docs/adr/ADR-safe-capability-language.md, status table"
   :state :done
   :done-note "Shipped 2026-07-08 as a deliberately NARROW slice, not a
               general Rust-style ownership/borrow/lifetime system: affine
               typing scoped ONLY to capability-typed values (deterministic
               drop, no implicit clone -- kotoba-lang/kotoba
               kotoba.runtime/cap-affine-problems, :cap-value-reused). A
               full ownership analysis was deliberately rejected -- per the
               ADR's own safety ladder it would only reach \"A grade\",
               below the capability-confinement (S grade) target already
               shipped; T1 Memory Safety was already achieved without a
               borrow checker (free-less allocator + bounds checks + no
               concurrency primitives). Documented conservative
               false-negative: tracking is per local binding name, so
               renaming through a let-alias and using both names once each
               is an uncaught reuse -- does not weaken runtime confinement
               (T3), since every use still re-resolves through
               kotoba.cap-table/resolve-use regardless of alias."}
  {:item ".cljs reinstated as an accepted compatibility source extension"
   :ref "kotoba-lang/kotoba-lang docs/adr/ADR-kotoba-lang-profile.md addendum (2026-07-08); profile v3"
   :state :done
   :done-note "Shipped 2026-07-08 across three repos: kotoba-core-contracts
               gained a :cljs source-kind (resources/kotoba/lang/
               source_contract.edn) mirroring .clj's shape -- single-target
               (:reader-target :cljs), NOT the fully portable .cljc branch
               chain, so a .cljs file cannot carry #?(:kotoba ...) branches
               any more than a .clj file can. kotoba-lang/kotoba bumped its
               pin and added src/demo.cljs as a genuine end-to-end positive
               fixture (a bare .cljs FILE, not .cljc read under
               --reader-target cljs, defaulting to the :cljs target with no
               flag needed). kotoba-lang/kotoba-lang bumped profile.edn to
               v3, added a dated ADR addendum documenting the reversal
               (the v2 retirement rested on a thin equivalence argument
               with no porting-friction analysis), and added a positive
               conformance fixture with expect-values verified against the
               real implementation, not just asserted."}]

 ;; --- where / how to post --------------------------------------------
 :post/target
 {:site "clojureverse.org"
  :category "Showcase"
  :category-correction
  "An earlier draft of this plan named a category ('Community News',
   explicitly avoiding 'Show and Tell') that does NOT exist on
   ClojureVerse -- an unverified guess, not checked against the actual
   forum. The real top-level categories (fetched from
   clojureverse.org/categories, 2026-07-08) are: Community Center,
   Questions & Help, Showcase, Learning Resources, Topics, Projects, The
   Fringe, Clojure Sustainability Works. There is no announcements/news
   category distinct from Showcase."
  :category-rationale
  "Showcase is where library/language [ANN] posts actually go on this
   forum -- confirmed from its real recent topics, which include '[ANN]
   Quandary' (a Clojure DSL), '[ANN Preview] PluMCP', and 'Clua 0.1.1 --
   safe execution of untrusted Lua scripts in Clojure' (close in spirit to
   Kotoba: a constrained/sandboxed execution story). The forum explicitly
   welcomes 'genuine instead of spammy' self-promotion there -- framing the
   post as an announcement with real design questions attached fits the
   norm, rather than avoiding the category altogether."
  :sequencing "Post here first; incorporate whatever critique lands before
               cross-posting to r/ProgrammingLanguages, Lobsters, or
               Show HN -- don't debut the same rough edges twice."}

 :post/title-options
 ["[ANN] Kotoba: a Clojure-shaped language that compiles to WebAssembly, with capability-based safety (feedback wanted)"
  "[ANN] Kotoba: capability-safe Wasm compilation for a Clojure-family source profile -- design feedback wanted"
  "[ANN] A Clojure-shaped, Wasm-targeting language for sandboxing untrusted/AI-generated code -- looking for design critique"]
 :post/title-recommended
 "[ANN] Kotoba: a Clojure-shaped language that compiles to WebAssembly, with capability-based safety (feedback wanted)"
 :post/title-rationale
 "Deliberately says 'Clojure-shaped', not 'Clojure' or 'Clojure on Clojure',
  in the title -- that framing belongs in the body where it can be
  explained, not in the title where it reads as a claim to litigate. The
  '[ANN]' prefix matches the real convention observed in Showcase's recent
  topics ('[ANN] Quandary', '[ANN Preview] PluMCP 0.2.0') -- not invented,
  copied from what other announcement posts on this forum actually do."

 :post/open-question-to-seed-discussion
 "We shipped a narrow affine-typing check scoped only to capability values
  instead of a general ownership/borrow system, on the reasoning that it's
  what the actual threat model needs and it's small enough to verify
  exhaustively. It has a documented gap (tracked per binding name, not per
  value) that doesn't weaken runtime enforcement but is a real limitation.
  Where do you draw the line between 'ship the narrow useful slice, gaps
  documented' and 'go build the complete thing first'?"

 :post/body-markdown
 "## What Kotoba is

Kotoba is a small **Clojure-shaped language profile** for compiling
capability-checked programs to WebAssembly. It's aimed at code that should
be inspectable, portable, and constrained: AI-generated cells, sandboxed
automation, repository policy, and other untrusted programs where the host
decides which capabilities are available.

Three things up front, since 'Clojure-shaped' invites the obvious pushback:

1. **It's a Clojure-family *profile/subset*, not a full Clojure or
   ClojureScript implementation.** Canonical source extension is `.kotoba`;
   `.clj`, `.cljc`, and `.cljs` are accepted as compatibility formats. It is
   not \"any JVM Clojure or ClojureScript program runs.\"
2. **Compilation target is WebAssembly, compile-first.** The public surface
   is `kotoba -e` / `kotoba wasm ...` -- not an interpreter with a Wasm
   backend bolted on.
3. **Capability-confined safety is the point, not an add-on.** Capabilities
   are explicit, scoped, typed values (never ambient strings a module can
   summon by name); a host-side guard intersects what a module was actually
   handed against policy before any call runs.

## 30-second tour

```sh
kotoba -e '(+ 1 2)'
kotoba wasm build examples/hello.kotoba -o hello.wasm
kotoba wasm safe-policy examples/policy-demo.kotoba
kotoba wasm safe-build examples/policy-demo.kotoba --policy policy.edn -o policy-demo.wasm
```

## A few design notes

The compiler, CLI, and conformance tooling that process this Clojure-shaped
language are themselves written in Clojure/ClojureScript -- an earlier Rust
implementation was fully retired in favor of this CLJC authority.

Safety is *benchmarked against* Rust, not copied from it: capability
confinement (deny-by-default, typed capabilities, signed audit receipts) is
designed to exceed Rust-style memory safety alone, not just match it.
Rather than a full ownership/borrow/lifetime system, we shipped a narrow
affine check scoped only to capability values -- a handle can be consumed
at most once per execution path -- with a documented limitation instead of
a broader, harder-to-verify mechanism.

## The rest of the stack

Alongside the language, [`kotoba-lang/kotobase`](https://github.com/kotoba-lang/kotobase)
plays the role Datomic plays for Clojure -- same datom/EAVT/Datalog model,
but reimagined for the distributed web: content-addressed blake3/CIDv1
blocks instead of a single transactor over SQL/DynamoDB, a Prolly Tree
instead of a B-tree, an immutable commit DAG instead of a single log, and a
CACAO-authenticated edge runtime instead of a JVM peer with a direct
storage connection.

## Where to look

- Language contract: https://github.com/kotoba-lang/kotoba-lang
- Compiler/runtime (CLJC, Rust-free): https://github.com/kotoba-lang/kotoba
- Distributed-web Datomic: https://github.com/kotoba-lang/kotobase

## Question for this community

We shipped a narrow affine-typing check scoped only to capability values
instead of a general ownership/borrow system, on the reasoning that it's
what the actual threat model needs and it's small enough to verify
exhaustively. It has a documented gap (tracked per binding name, not per
value) that doesn't weaken runtime enforcement but is a real limitation.
Where do you draw the line between \"ship the narrow useful slice, gaps
documented\" and \"go build the complete thing first\"?"

 :post/body-markdown-ja
 "## Kotoba とは

Kotoba は capability-checked なプログラムを WebAssembly にコンパイルする、
小さな **Clojure 形の言語プロファイル** です。AI 生成コード片・サンドボックス化
された自動化・その他ホスト側が capability を決める未信頼プログラムなど、検査
可能・移植可能・制約されたコードを想定しています。

最初に3点だけ('Clojure 形'と聞くと当然出てくる疑問への先回り):

1. **Clojure ファミリーの *プロファイル/サブセット* であり、フルの Clojure /
   ClojureScript 実装ではありません。** 正準な拡張子は `.kotoba`。`.clj` /
   `.cljc` / `.cljs` は互換フォーマットとして受理されます。「JVM Clojure /
   ClojureScript のプログラムなら何でも動く」わけではありません。
2. **コンパイル先は WebAssembly、compile-first です。** 公開インターフェース
   は `kotoba -e` / `kotoba wasm ...` — Wasm バックエンドを後付けした
   インタプリタではありません。
3. **capability に閉じ込められた安全性が本体で、後付けの機能ではありません。**
   capability は明示的・スコープ付き・型付きの値であり、モジュールが名前で
   勝手に呼び出せる ambient な文字列ではありません。呼び出しの前にホスト側の
   ガードが「実際に渡されたもの」をポリシーと突き合わせます。

## 30秒ツアー

```sh
kotoba -e '(+ 1 2)'
kotoba wasm build examples/hello.kotoba -o hello.wasm
kotoba wasm safe-policy examples/policy-demo.kotoba
kotoba wasm safe-build examples/policy-demo.kotoba --policy policy.edn -o policy-demo.wasm
```

## 設計上の補足

このClojure形言語を処理するコンパイラ・CLI・conformance ツール群自体が
Clojure/ClojureScript で書かれています — 以前あった Rust 実装は完全に撤去し、
この CLJC 実装が正本になりました。

安全性は Rust を**参考にした**のではなく、Rust に対して**ベンチマークして
上回ることを狙って**設計されています: capability confinement（deny-by-default、
型付き capability、署名済み監査レシート）は、Rust 流のメモリ安全性だけを
上回ることを目指しています。汎用的な所有権/借用/lifetime システムの代わりに、
capability の値だけに限定した狭い affine チェックを採用しました — ハンドルは
実行パスごとに高々一度しか消費できません。より広く検証しづらい仕組みの代わりに、
限界を明記した狭いチェックを選びました。

## スタックの残り

言語のほかに、[`kotoba-lang/kotobase`](https://github.com/kotoba-lang/kotobase)
が Clojure に対する Datomic の役割を果たします — 同じ datom/EAVT/Datalog
モデルですが、分散webのために再設計されています: 単一のtransactor + SQL/
DynamoDB ではなく blake3/CIDv1 のコンテンツアドレス方式、B-tree ではなく
Prolly Tree、単一ログではなく immutable な commit DAG、直接ストレージ接続を
持つ JVM peer ではなく CACAO 認証された edge runtime、という違いです。

## 参照先

- 言語コントラクト: https://github.com/kotoba-lang/kotoba-lang
- コンパイラ/ランタイム(CLJC、Rust-free): https://github.com/kotoba-lang/kotoba
- 分散webのDatomic: https://github.com/kotoba-lang/kotobase

## このコミュニティへの質問

汎用的な所有権/借用システムの代わりに、capability の値だけに限定した狭い
affine チェックを採用しました — これは実際の脅威モデルが要求するものであり、
かつ網羅的に検証しきれる規模に収まるという判断です。既知のギャップも1つ
あります(束縛名単位の追跡であり値のprovenance単位ではないため、runtime側の
enforcementは弱めないものの実在する限界です)。「制約を明記した上で狭く
有用なスライスを出す」のと「まず完全なものを作ってから出す」の境界を、
皆さんはどこに引きますか?"

 :post/posting-guidance
 ["Category: Showcase -- verified against clojureverse.org/categories, where [ANN] library/language posts actually go on this forum."
  "Title says 'Clojure-shaped', never 'Clojure on Clojure' or bare 'Clojure' -- that framing lives in the body."
  "First 3 lines state what's different (profile/subset, Wasm compile-first, capability-confined safety) before anything else."
  "Include at least one runnable code block (the 30-second tour)."
  "End with one concrete, answerable question (the narrow-slice-vs-complete-feature tradeoff) to drive replies."
  "Link kotoba-lang/kotoba-lang (the contract) before kotoba-lang/kotoba (the implementation) -- resolve 'what is this' before 'how does it run'."
  "Only cross-post to r/ProgrammingLanguages / Lobsters / Show HN after incorporating ClojureVerse feedback."]}
