{:post/id "kotoba-clojureverse-launch"
 :post/status :draft
 :post/created "2026-07-08"

 ;; --- publish gate ---------------------------------------------------
 ;; Do not post until every item here is either :done or explicitly
 ;; downgraded to a roadmap bullet in the post body (no overclaiming).
 :post/blocked-by
 [{:item "Rust-style borrow checker (S2 layer)"
   :ref "kotoba-lang/kotoba-lang docs/adr/ADR-safe-capability-language.md, status table"
   :state :not-started
   :why "Currently listed as ⬜ (planned, not implemented). Want at least a
         working prototype so the safety-ladder claim in the post reads as
         partially delivered rather than pure roadmap."}
  {:item ".cljs reinstated as an accepted compatibility source extension"
   :ref "kotoba-lang/kotoba-lang docs/adr/ADR-kotoba-lang-profile.md (profile v2 retired it)"
   :state :not-started
   :design-questions
   ["reader-target fallback chain for .cljs files: needs its own
     :kotoba -> :cljs -> :default chain, distinct from .clj/.cljc's
     :kotoba -> :clj -> :default (reusing the .clj/.cljc chain would leave
     the :cljs branch dead code)."
    "accepted-extensions is part of the versioned lang/profile.edn contract
     -> this is a profile v2 -> v3 bump, not a quiet patch (see
     docs/lang/versioning.md M0-M6 policy)."
    "needs a new positive conformance fixture in lang/conformance/ (a .cljs
     file that resolves and compiles); today only a generic
     :negative-unsupported-extension fixture exists, nothing .cljs-specific."]
   :why "Lowers porting friction for people who already have .cljs code and
         want to try Kotoba without renaming files first. The original
         retirement rationale (profile v2, 2026-07-02, commit a11b7eb9) was a
         thin equivalence argument only (\".cljc + #?(:cljs ...) already
         covers what a dedicated .cljs file would\") with no documented
         discussion of porting-friction trade-offs, so reinstating is not
         fighting a deep design decision -- it's revisiting a fast cleanup
         call made incidentally during the Rust-removal pass."}]

 ;; --- where / how to post --------------------------------------------
 :post/target
 {:site "clojureverse.org"
  :category "Community News (not Show-and-Tell)"
  :category-rationale
  "Framing this as 'want feedback on design decisions' rather than 'look
   what I built' fits how ClojureVerse engages with new-language launches
   -- it invites judgment against Clojure's own design norms instead of a
   pure announcement."
  :sequencing "Post here first; incorporate whatever critique lands before
               cross-posting to r/ProgrammingLanguages, Lobsters, or
               Show HN -- don't debut the same rough edges twice."}

 :post/title-options
 ["Kotoba: a Clojure-shaped language that compiles to WebAssembly, with capability-based safety (feedback wanted)"
  "Kotoba: capability-safe Wasm compilation for a Clojure-family source profile -- design feedback wanted"
  "A Clojure-shaped, Wasm-targeting language for sandboxing untrusted/AI-generated code -- looking for design critique"]
 :post/title-recommended
 "Kotoba: a Clojure-shaped language that compiles to WebAssembly, with capability-based safety (feedback wanted)"
 :post/title-rationale
 "Deliberately says 'Clojure-shaped', not 'Clojure' or 'Clojure on Clojure',
  in the title -- that framing belongs in the body where it can be
  explained, not in the title where it reads as a claim to litigate."

 :post/open-question-to-seed-discussion
 "We retired dedicated .cljs source files in profile v2 in favor of
  requiring #?(:cljs ...) inside .cljc, on the argument that the two are
  redundant. We're reconsidering that for porting friction. Anyone here
  ported a real .cljs codebase into a .cljc-only source contract -- was the
  friction as low as the redundancy argument assumes?"

 :post/body-markdown
 "## What Kotoba is

Kotoba is a small **Clojure-shaped language profile** for compiling
capability-checked programs to WebAssembly. It's aimed at code that should
be inspectable, portable, and constrained: AI-generated cells, sandboxed
automation, repository policy, and other untrusted programs where the host
decides which capabilities are available.

Three things up front, since 'Clojure-shaped' invites the obvious pushback:

1. **It's a Clojure-family *profile/subset*, not a full Clojure or
   ClojureScript implementation.** Canonical source extension is `.kotoba`;
   `.clj` and `.cljc` are accepted as compatibility formats, with
   Kotoba-specific behavior selected via a `:kotoba` reader-conditional
   branch. It is not \"any JVM Clojure or ClojureScript program runs.\"
2. **Compilation target is WebAssembly, compile-first.** The public surface
   is `kotoba -e` / `kotoba wasm ...` -- not an interpreter with a Wasm
   backend bolted on.
3. **Capability-confined safety is the point, not an add-on.** Capabilities
   are explicit, scoped, typed values (never ambient strings a module can
   summon by name); a host-side guard intersects what a module was actually
   handed against policy before any call runs.

## 30-second tour

```sh
kotoba -e '(+ 1 2)'
kotoba wasm build examples/hello.kotoba -o hello.wasm
kotoba wasm safe-policy examples/policy-demo.kotoba
kotoba wasm safe-build examples/policy-demo.kotoba --policy policy.edn -o policy-demo.wasm
```

## The implementation is itself Clojure

The compiler, CLI, and conformance tooling that process this Clojure-shaped
language are themselves written in Clojure/ClojureScript (`.cljc`) -- an
earlier Rust implementation was fully retired in favor of this CLJC
authority. Its capability-safety design is *benchmarked against* Rust's
ownership/borrow-safety model on an explicit safety ladder, not modeled on
or ported from it: Rust-style memory safety is treated as an insufficient
baseline that Kotoba's capability-confinement model (deny-by-default,
typed capabilities, signed audit receipts) is designed to exceed. A
Rust-style borrow checker is [in progress / shipped -- update before
posting] as an additional layer.

## The rest of the stack

Alongside the language, [`kotoba-lang/kotobase`](https://github.com/kotoba-lang/kotobase)
plays the role Datomic plays for Clojure -- same datom/EAVT/Datalog model,
but reimagined for the distributed web: content-addressed blake3/CIDv1
blocks instead of a single transactor over SQL/DynamoDB, a Prolly Tree
instead of a B-tree, an immutable commit DAG instead of a single log, and a
CACAO-authenticated edge runtime instead of a JVM peer with a direct
storage connection.

## Where to look

- Language contract: https://github.com/kotoba-lang/kotoba-lang
- Compiler/runtime (CLJC, Rust-free): https://github.com/kotoba-lang/kotoba
- Distributed-web Datomic: https://github.com/kotoba-lang/kotobase

## Question for this community

We retired dedicated `.cljs` source files in profile v2 in favor of
requiring `#?(:cljs ...)` inside `.cljc`, on the argument that the two are
redundant. We're reconsidering that for porting friction. Anyone here
ported a real `.cljs` codebase into a `.cljc`-only source contract -- was
the friction as low as the redundancy argument assumes?"

 :post/posting-guidance
 ["Category: Community News, not Show-and-Tell -- invites design judgment, not just applause."
  "Title says 'Clojure-shaped', never 'Clojure on Clojure' or bare 'Clojure' -- that framing lives in the body."
  "First 3 lines state what's different (profile/subset, Wasm compile-first, capability-confined safety) before anything else."
  "Include at least one runnable code block (the 30-second tour)."
  "End with one concrete, answerable technical question (the .cljs one above) to drive replies."
  "Link kotoba-lang/kotoba-lang (the contract) before kotoba-lang/kotoba (the implementation) -- resolve 'what is this' before 'how does it run'."
  "Only cross-post to r/ProgrammingLanguages / Lobsters / Show HN after incorporating ClojureVerse feedback."]}
