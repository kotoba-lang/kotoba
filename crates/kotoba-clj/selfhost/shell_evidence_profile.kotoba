(defn profile-count [] 3)

(defn required-command-count [] 25)

(defn required-evidence-stem-count [] 31)

(defn profile-list-digest [] 614)

(defn required-command-digest [] 4822)

(defn required-evidence-stem-digest [] 61285)

(defn contract-score []
  (+ (* (profile-count) 10000)
     (* (required-command-count) 100)
     (required-evidence-stem-count)))
