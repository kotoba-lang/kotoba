[package]
name = "kotoba-turn"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Kotoba TURN relay (RFC 8656) — socket-free core: ephemeral-credential auth (HMAC-SHA1, coturn use-auth-secret), STUN codec (header/attributes/XOR-MAPPED-ADDRESS/MESSAGE-INTEGRITY/FINGERPRINT), allocation state machine, ChannelData framing, and the request auth/demux gate. The UDP/TCP listener shell is the only remaining layer. See docs/ADR-turn-relay.md."

[dependencies]
hmac      = { workspace = true }
sha1      = { workspace = true }
base64    = { workspace = true }
thiserror = { workspace = true }
