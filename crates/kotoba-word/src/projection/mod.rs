//! Projections — generated external faces of a root. Never hand-written:
//! both are derived from the same word definitions (and therefore from the
//! same type signatures).

pub mod lexicon;
pub mod mcp;
