[package]
name = "bsp-counter"
version = "0.1.0"
edition = "2021"
[lib]
crate-type = ["cdylib"]
[dependencies]
wit-bindgen = "0.36"
ciborium = "0.2"
serde = { version = "1", features = ["derive"] }
[package.metadata.component]
package = "spike:bsp"
[package.metadata.component.target]
world = "bsp"

[workspace]
