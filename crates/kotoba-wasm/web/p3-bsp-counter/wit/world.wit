package spike:bsp@0.1.0;
/// A multi-superstep BSP guest: the JS driver re-feeds run()'s output as the
/// next input while status == "continue".
world bsp {
  import host-log: func(msg: string);
  export run: func(state-cbor: list<u8>) -> result<list<u8>, string>;
}
